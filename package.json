{
  "name": "block-color-puzzle",
  "version": "1.0.0",
  "private": true,
  "description": "A sliding-block colour-sorting puzzle, built as an installable PWA.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit.test.mjs tests/integration.test.mjs",
    "test:unit": "node --test tests/unit.test.mjs",
    "test:integration": "node --test tests/integration.test.mjs",
    "test:layout": "node --test tests/layout.test.mjs",
    "test:all": "node --test \"tests/*.test.mjs\"",
    "serve": "npx --yes http-server -p 8123 -c-1 ."
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "playwright": "1.63.0-alpha-2026-07-28"
  },
  "engines": {
    "node": ">=20"
  }
}
